{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "C++ LLDB json default",
        "program": "${workspaceFolder}/*_debug",
        "cwd": "${workspaceFolder}",
        "args": [],
    }
    ]
}
